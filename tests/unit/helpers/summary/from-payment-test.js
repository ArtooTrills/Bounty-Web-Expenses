import { summaryFromPayment } from '../../../../helpers/summary/from-payment';
import { module, test } from 'qunit';

module('Unit | Helper | summary/from payment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summaryFromPayment(42);
  assert.ok(result);
});
