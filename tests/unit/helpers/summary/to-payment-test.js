import { summaryToPayment } from '../../../../helpers/summary/to-payment';
import { module, test } from 'qunit';

module('Unit | Helper | summary/to payment');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summaryToPayment(42);
  assert.ok(result);
});
