import { summaryTotalOwned } from '../../../../helpers/summary/total-owned';
import { module, test } from 'qunit';

module('Unit | Helper | summary/total owned');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summaryTotalOwned(42);
  assert.ok(result);
});
