import { summaryTotalOwes } from '../../../../helpers/summary/total-owes';
import { module, test } from 'qunit';

module('Unit | Helper | summary/total owes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summaryTotalOwes(42);
  assert.ok(result);
});
