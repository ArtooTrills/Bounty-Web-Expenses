import { summaryTotalSpent } from '../../../../helpers/summary/total-spent';
import { module, test } from 'qunit';

module('Unit | Helper | summary/total spent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summaryTotalSpent(42);
  assert.ok(result);
});
