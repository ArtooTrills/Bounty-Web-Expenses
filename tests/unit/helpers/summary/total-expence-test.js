import { summaryTotalExpence } from '../../../../helpers/summary/total-expence';
import { module, test } from 'qunit';

module('Unit | Helper | summary/total expence');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summaryTotalExpence(42);
  assert.ok(result);
});
