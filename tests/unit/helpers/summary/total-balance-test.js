import { summaryTotalBalance } from '../../../../helpers/summary/total-balance';
import { module, test } from 'qunit';

module('Unit | Helper | summary/total balance');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summaryTotalBalance(42);
  assert.ok(result);
});
