import { summaryAbsoluteAmount } from '../../../../helpers/summary/absolute-amount';
import { module, test } from 'qunit';

module('Unit | Helper | summary/absolute amount');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summaryAbsoluteAmount(42);
  assert.ok(result);
});
