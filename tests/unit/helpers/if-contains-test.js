import { ifContains } from '../../../helpers/if-contains';
import { module, test } from 'qunit';

module('Unit | Helper | if contains');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifContains(42);
  assert.ok(result);
});
