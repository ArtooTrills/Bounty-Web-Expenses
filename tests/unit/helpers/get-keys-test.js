import { getKeys } from '../../../helpers/get-keys';
import { module, test } from 'qunit';

module('Unit | Helper | get keys');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getKeys(42);
  assert.ok(result);
});
