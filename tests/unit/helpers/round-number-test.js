import { roundNumber } from '../../../helpers/round-number';
import { module, test } from 'qunit';

module('Unit | Helper | round number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = roundNumber(42);
  assert.ok(result);
});
