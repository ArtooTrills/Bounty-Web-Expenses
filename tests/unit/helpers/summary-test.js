import { summary } from '../../../helpers/summary';
import { module, test } from 'qunit';

module('Unit | Helper | summary');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = summary(42);
  assert.ok(result);
});
